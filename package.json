{
  "name": "runit-pitch-deck",
  "version": "1.0.0",
  "description": "RunIt Interactive Pitch Deck",
  "scripts": {
    "dev": "bun run --watch index.ts",
    "start": "bun run index.ts",
    "build": "bun run build.ts"
  },
  "dependencies": {
    "htmx.org": "^2.0.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
